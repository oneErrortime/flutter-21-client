<?xml version="1.0" encoding="utf-8"?>
<!--
  Android Network Security Config
  Allows cleartext HTTP ONLY to emulator's host machine (10.0.2.2) during development.
  In production (RELEASE builds), all traffic goes over HTTPS automatically.
  Never allow cleartext in production.
-->
<network-security-config>

    <!-- Production: HTTPS only, certificate pinning recommended -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">yourapp.com</domain>
        <!-- Optional: Certificate pinning (update hash when cert renews)
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=</pin>
        </pin-set>
        -->
    </domain-config>

    <!-- Development ONLY: allow HTTP to Android emulator host -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

</network-security-config>
